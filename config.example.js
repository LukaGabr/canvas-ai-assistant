// Copy this file to config.js and fill in your own key.
// config.js is gitignored — never commit a real API key.
//
// This is a stopgap for local development. Stage 5 will replace it with a
// settings screen where each user enters their own Claude API key, stored in
// chrome.storage.local instead of a source file.
export const CLAUDE_API_KEY = "sk-ant-your-key-here";
